# Fixed build notes

- Hardened `AuthContext` against malformed/stale `localStorage` session JSON.
- Added a React runtime error boundary with a recovery action.
- Fixed demo authentication so the demo state is persisted before protected dashboard routing.
- Added recruiter-friendly live demo access from the landing page for both User and Admin experiences.
- Kept the PRD-driven landing-page scroll reveal, glassmorphism, motion and responsive design.
