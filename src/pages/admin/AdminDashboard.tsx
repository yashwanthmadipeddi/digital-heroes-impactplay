import { Activity, BarChart3, HeartHandshake, Users, Trophy, ArrowRight } from 'lucide-react';
import { Link } from 'react-router-dom';
import PageHeader from '../../components/PageHeader';
import SectionReveal from '../../components/SectionReveal';
import { loadDemoState } from '../../lib/demoData';

export default function AdminDashboard(){const state=loadDemoState(true);return <div><PageHeader eyebrow="ADMIN CONTROL ROOM" title="Everything operational, at a glance." body="Manage members, charities, draws, winner verification, and reporting from a single workspace."/><div className="container admin-page"><div className="dashboard-grid">{[['Total users','1,684',Users],['Active subscribers','1,248',Activity],['Prize pool','₹62.4k',Trophy],['Charity contributions','₹24.0k',HeartHandshake]].map(([label,value,Icon],i)=>{const C=Icon as React.ComponentType<{size?:number}>;return <SectionReveal key={String(label)} delay={i*.04}><div className="metric-card glass-card"><span>{String(label)}</span><strong>{String(value)}</strong><small><C size={14}/> +8.4% this month</small></div></SectionReveal>})}</div><div className="admin-link-grid">{[["Users","Manage profiles, subscriptions and access","/admin/users",Users],["Charities","Curate directory content and featured causes","/admin/charities",HeartHandshake],["Draws","Simulate, calculate, publish","/admin/draws",Trophy],["Winners","Verify proof and mark payouts","/admin/winners",CheckIcon],["Reports","Review pool, participation and impact trends","/admin/reports",BarChart3]].map(([title,body,to,Icon],i)=>{const C=Icon as React.ComponentType<{size?:number}>;return <SectionReveal key={String(title)} delay={i*.03}><Link to={String(to)} className="admin-nav-card glass-card"><C size={20}/><div><strong>{String(title)}</strong><span>{String(body)}</span></div><ArrowRight size={16}/></Link></SectionReveal>})}</div></div></div>}
function CheckIcon(){return <Trophy size={20}/>}
