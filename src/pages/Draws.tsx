import { motion } from 'framer-motion';
import { ArrowRight, CalendarClock, CircleHelp, ShieldCheck } from 'lucide-react';
import PageHeader from '../components/PageHeader';
import SectionReveal from '../components/SectionReveal';
import Pill from '../components/Pill';
import { loadDemoState } from '../lib/demoData';

export default function Draws(){const state=loadDemoState();return <div><PageHeader eyebrow="THE MONTHLY REWARD DRAW" title="Transparent by design." body="Each month has a visible draw type, eligible member count, generated numbers, and prize-pool math. Five-number rewards roll over when unclaimed."/><div className="container draw-page"><SectionReveal><div className="draw-hero glass-card"><div className="draw-hero-copy"><span className="eyebrow">NEXT DRAW · OCTOBER 2026</span><h2>{state.draws[1].numbers.map(n=>n.toString().padStart(2,'0')).join(' · ')}</h2><div className="hero-number-caption">simulated reward numbers</div><div className="draw-meta"><Pill tone="orange">SIMULATED</Pill><span><CalendarClock size={14}/> Publication controlled by admin</span><span><ShieldCheck size={14}/> Auditable prize logic</span></div></div><div className="draw-big-stat"><span>Reward pool basis</span><strong>₹1,00,000</strong><small>example month · 40/35/25 allocation</small></div></div></SectionReveal><div className="prize-grid">{[['5-number','40%','₹40,000','Jackpot rollover'],['4-number','35%','₹35,000','Split equally'],['3-number','25%','₹25,000','Split equally']].map((x,i)=><SectionReveal key={x[0]} delay={i*.06}><motion.div className={`prize-card glass-card ${i===0?'featured':''}`} whileHover={{y:-5}}><span>{x[0]}</span><strong>{x[1]}</strong><small>{x[2]}</small><p>{x[3]}</p></motion.div></SectionReveal>)}</div><SectionReveal><div className="info-grid"><div className="panel glass-card"><div className="panel-head"><div><span className="eyebrow">DRAW TYPES</span><h2>Two operational modes</h2></div><CircleHelp size={18}/></div><div className="info-row"><strong>Random</strong><span>Standard lottery-style unique numbers from 1–45.</span></div><div className="info-row"><strong>Algorithmic</strong><span>Weighted from observed member Form Scores with a transparent documented rule.</span></div></div><div className="panel glass-card"><div className="panel-head"><div><span className="eyebrow">OPERATIONS</span><h2>Admin-controlled lifecycle</h2></div></div>{['Create draft','Run simulation','Review numbers + pools','Publish','Verify winners','Track payouts'].map((x,i)=><div className="timeline-row" key={x}><span>{String(i+1).padStart(2,'0')}</span>{x}<ArrowRight size={13}/></div>)}</div></div></SectionReveal></div></div>}
